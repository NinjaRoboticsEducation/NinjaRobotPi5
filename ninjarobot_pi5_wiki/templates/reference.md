---
type: Reference
title: "{{ title }}"
description: "{{ description }}"
resource: "{{ resource }}"
tags: []
status: stable
generated:
  by: "{{ actor }}"
  at: "{{ timestamp }}"
sources:
  - id: "{{ source_id }}"
    resource: "{{ resource }}"
    title: "{{ title }}"
    content_hash: "{{ source_hash }}"
---

# {{ title }}

## Source Summary

Summarize the source without copying long passages.

## Key Contributions

List the ideas or evidence this source adds to the wiki.

## Limitations

Record scope, uncertainty, bias, or missing context.
